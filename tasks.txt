Read CLAUDE.md before starting. Read the current app.js and index.html in full before writing any code. You are making two small fixes. Do not touch style.css, database.js or the Excel file unless strictly necessary.

---

TASK 1 — ADD LEARN TO WELCOME SCREEN

In the showWelcomeScreen() function in app.js, the welcome screen shows 6 mode buttons. Learn mode is missing. Add 'learn' to the modeNames object and add a Learn button as the FIRST button in the welcome-modes grid in index.html.

In index.html, find the welcome-modes div and add Learn as the first button:
<button class="welcome-mode-btn" onclick="startFromWelcome('learn')">Learn</button>

The button label must use the translation system so it updates when language changes. Update showWelcomeScreen() to include learn in the modeNames map:
learn: T('learn')

For users with zero items practiced (brand new profile), highlight the Learn button with a special style — green border and subtle green background — to suggest it as the starting point. Add a small label beneath it: "New? Start here" (translated in all 4 languages).

Add translation key 'newStartHere':
- en: 'New? Start here'
- nl: 'Nieuw? Begin hier'
- de: 'Neu? Fang hier an'
- fr: 'Nouveau ? Commence ici'

The highlight should only appear when the user has 0 items practiced. If they have any practice history the Learn button looks the same as all other buttons.

---

TASK 2 — FIX HELP TEXT FOR LEARN MODE

In the ensureHelpBtn() function or wherever help text is assigned per mode, make sure Learn mode has its own help text using the helpLearn translation key that was added in the previous session.

Verify the ? button in Learn mode shows: 'Watch each word pair carefully. After 5 pairs you will be asked to match them from memory.'

---

After both tasks: update CLAUDE.md. Do not modify database.js or French_Learning_Content_Database_v7.xlsx.
